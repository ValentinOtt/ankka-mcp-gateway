// The shipped Worker is the single canonical implementation.
export {
  TeamAccessError, normalizeTeamAccessRequest, planTeamAccessChange, teamPolicy, teamPolicyMatches,
} from '../payload/worker/index.js';
